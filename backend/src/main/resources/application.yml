server:
  port: 8080

app:
  # 默认使用 localhost，生产环境可以通过环境变量 APP_FRONTEND_URL 覆盖
  frontend-url: ${APP_FRONTEND_URL:http://localhost:5173}

file:
  upload-dir: uploads
  base-url: /uploads/

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/shopping?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:HuAng@2004}
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: ${SPRING_REDIS_HOST:localhost}
    port: ${SPRING_REDIS_PORT:6379}
    database: 0
  mail:
    host: ${SPRING_MAIL_HOST:smtp.qq.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:2323718705@qq.com}
    password: ${SPRING_MAIL_PASSWORD:nouydxvfxcheeaeb}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: ${JWT_SECRET:your_jwt_secret_key_must_be_long_enough_for_security_reasons_at_least_32_bytes}
  expiration: 86400000 # 24 hours

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
